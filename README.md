# Primary AI Model Configuration
PRIMARY_MODEL=gpt4-vision  # Default AI model to use (fallback if not specified)

# Model-specific Configuration
OPENAI_API_KEY=your_key    # Required for GPT-4 Vision model
XAI_API_KEY=your_key       # Required for Grok Vision model
OLLAMA_HOST=localhost:11434 # Ollama API host for LLaVA and Llama Vision

# Logging Configuration
DOCFLOW_LOG_LEVEL=ERROR    # Set to DEBUG for verbose output